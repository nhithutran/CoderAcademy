import React from "react";

const Nav = () => {
    return (
      <nav>
        <a href="/">Home</a>
        <a href="/">About me</a>
        <a href="/">Project</a>
        <a href="/">Contact</a>
      </nav>
    )
}

export default Nav; // To use it in another file